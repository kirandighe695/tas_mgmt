import React from "react";
import { Typography } from "@mui/material";
import '../../styles/Footer.scss';

const Footer = () => (
  <Typography className="footer">
    <p className="pt-4 ml-3">@Made with React using material UI</p>
  </Typography>
);

export default Footer;
